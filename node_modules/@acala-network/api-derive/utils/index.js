"use strict";

exports.__esModule = true;
exports.getAllCollateralCurrencyIds = getAllCollateralCurrencyIds;

var _operators = require("rxjs/operators");

function getAllCollateralCurrencyIds(api) {
  return api.query.cdpEngine.collateralParams.keys().pipe((0, _operators.map)(keys => {
    return keys.map(item => item.args[0]);
  }));
}