import { ApiInterfaceRx } from '@polkadot/api/types';
import { AccountId, Balance } from '@open-web3/orml-types/interfaces';
import { Observable } from 'rxjs';
export declare const balance: (instanceId: string, api: ApiInterfaceRx) => (address: AccountId | string | Uint8Array, token: any) => Observable<Balance>;
